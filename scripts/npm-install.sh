# these scripts is used with hooks in appspec.yml file
# if we plan to use hooks and do npm install and npm start in deploy
# stage this will be useful

#!/bin/bash
cd /home/ubuntu/shell
sudo npm install